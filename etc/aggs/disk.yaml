alarm:
  # 报警描述信息
  comment: 可能是日志过多

  # 默认使用率报警阈值, >=, (无指定盘符配置时, 针对所有盘符报警)
  percent: 80

  # 指定盘符报警(盘符只能为小写), 配置优先使用
  disk_symbol:
    # 可指定单独盘符的报警阈值
    c:
      percent: 90
    # 使用公共报警阈值
    d: